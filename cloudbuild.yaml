steps:
  # Docker Build
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', gcr.io/boxwood-bliss-375813/awesome-project:$SHORT_SHA', '.' ]
    
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', '-t', gcr.io/boxwood-bliss-375813/awesome-project:$SHORT_SHA' ]
          
# just for testing
